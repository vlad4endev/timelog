# Caddy reverse proxy example for production TLS
# Usage: caddy run --config docker/Caddyfile
# Requires DNS pointing to this server.

{$PUBLIC_HOST:localhost} {
	encode gzip
	reverse_proxy localhost:8080
}
