# Caddy inside Docker (profile tls) — automatic HTTPS via Let's Encrypt
# Set PUBLIC_HOST in .env (e.g. timelog.example.com), DNS must point to this server.

{$PUBLIC_HOST} {
	encode gzip
	reverse_proxy web:80
}
